{-# LANGUAGE CPP #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE LambdaCase #-}
#if __GLASGOW_HASKELL__ <= 708
{-# LANGUAGE OverlappingInstances #-}
#endif

{-# OPTIONS_GHC -fno-warn-incomplete-patterns #-}

-- | Pretty-printer for Grammar.
--   Generated by the BNF converter.

module Grammar.Print where

import Prelude
  ( ($), (.)
  , Bool(..), (==), (<)
  , Int, Integer, Double, (+), (-), (*)
  , String, (++)
  , ShowS, showChar, showString
  , all, dropWhile, elem, foldr, id, map, null, replicate, shows, span
  )
import Data.Char ( Char, isSpace )
import qualified Grammar.Abs

-- | The top-level printing method.

printTree :: Print a => a -> String
printTree = render . prt 0

type Doc = [ShowS] -> [ShowS]

doc :: ShowS -> Doc
doc = (:)

render :: Doc -> String
render d = rend 0 (map ($ "") $ d []) "" where
  rend i = \case
    "["      :ts -> showChar '[' . rend i ts
    "("      :ts -> showChar '(' . rend i ts
    "{"      :ts -> showChar '{' . new (i+1) . rend (i+1) ts
    "}" : ";":ts -> new (i-1) . space "}" . showChar ';' . new (i-1) . rend (i-1) ts
    "}"      :ts -> new (i-1) . showChar '}' . new (i-1) . rend (i-1) ts
    [";"]        -> showChar ';'
    ";"      :ts -> showChar ';' . new i . rend i ts
    t  : ts@(p:_) | closingOrPunctuation p -> showString t . rend i ts
    t        :ts -> space t . rend i ts
    _            -> id
  new i     = showChar '\n' . replicateS (2*i) (showChar ' ') . dropWhile isSpace
  space t s =
    case (all isSpace t', null spc, null rest) of
      (True , _   , True ) -> []              -- remove trailing space
      (False, _   , True ) -> t'              -- remove trailing space
      (False, True, False) -> t' ++ ' ' : s   -- add space if none
      _                    -> t' ++ s
    where
      t'          = showString t []
      (spc, rest) = span isSpace s

  closingOrPunctuation :: String -> Bool
  closingOrPunctuation [c] = c `elem` closerOrPunct
  closingOrPunctuation _   = False

  closerOrPunct :: String
  closerOrPunct = ")],;"

parenth :: Doc -> Doc
parenth ss = doc (showChar '(') . ss . doc (showChar ')')

concatS :: [ShowS] -> ShowS
concatS = foldr (.) id

concatD :: [Doc] -> Doc
concatD = foldr (.) id

replicateS :: Int -> ShowS -> ShowS
replicateS n f = concatS (replicate n f)

-- | The printer class does the job.

class Print a where
  prt :: Int -> a -> Doc
  prtList :: Int -> [a] -> Doc
  prtList i = concatD . map (prt i)

instance {-# OVERLAPPABLE #-} Print a => Print [a] where
  prt = prtList

instance Print Char where
  prt     _ s = doc (showChar '\'' . mkEsc '\'' s . showChar '\'')
  prtList _ s = doc (showChar '"' . concatS (map (mkEsc '"') s) . showChar '"')

mkEsc :: Char -> Char -> ShowS
mkEsc q = \case
  s | s == q -> showChar '\\' . showChar s
  '\\' -> showString "\\\\"
  '\n' -> showString "\\n"
  '\t' -> showString "\\t"
  s -> showChar s

prPrec :: Int -> Int -> Doc -> Doc
prPrec i j = if j < i then parenth else id

instance Print Integer where
  prt _ x = doc (shows x)

instance Print Double where
  prt _ x = doc (shows x)

instance Print Grammar.Abs.Ident where
  prt _ (Grammar.Abs.Ident i) = doc $ showString i

instance Print Grammar.Abs.Program where
  prt i = \case
    Grammar.Abs.Program topdefs -> prPrec i 0 (concatD [prt 0 topdefs])

instance Print Grammar.Abs.TopDef where
  prt i = \case
    Grammar.Abs.FnDef type_ id_ args block -> prPrec i 0 (concatD [prt 0 type_, prt 0 id_, doc (showString "("), prt 0 args, doc (showString ")"), prt 0 block])
    Grammar.Abs.GlobDecl type_ items -> prPrec i 0 (concatD [prt 0 type_, prt 0 items, doc (showString ";")])
    Grammar.Abs.GlobFinDecl type_ items -> prPrec i 0 (concatD [doc (showString "final"), prt 0 type_, prt 0 items, doc (showString ";")])
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print [Grammar.Abs.TopDef] where
  prt = prtList

instance Print Grammar.Abs.Arg where
  prt i = \case
    Grammar.Abs.Arg type_ id_ -> prPrec i 0 (concatD [prt 0 type_, prt 0 id_])
  prtList _ [] = concatD []
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [Grammar.Abs.Arg] where
  prt = prtList

instance Print Grammar.Abs.Block where
  prt i = \case
    Grammar.Abs.Block stmts -> prPrec i 0 (concatD [doc (showString "{"), prt 0 stmts, doc (showString "}")])

instance Print [Grammar.Abs.Stmt] where
  prt = prtList

instance Print Grammar.Abs.Stmt where
  prt i = \case
    Grammar.Abs.Empty -> prPrec i 0 (concatD [doc (showString ";")])
    Grammar.Abs.BStmt block -> prPrec i 0 (concatD [prt 0 block])
    Grammar.Abs.Decl type_ items -> prPrec i 0 (concatD [prt 0 type_, prt 0 items, doc (showString ";")])
    Grammar.Abs.DeclFinal type_ items -> prPrec i 0 (concatD [doc (showString "final"), prt 0 type_, prt 0 items, doc (showString ";")])
    Grammar.Abs.Ass id_ expr -> prPrec i 0 (concatD [prt 0 id_, doc (showString "="), prt 0 expr, doc (showString ";")])
    Grammar.Abs.Incr id_ -> prPrec i 0 (concatD [prt 0 id_, doc (showString "++"), doc (showString ";")])
    Grammar.Abs.Decr id_ -> prPrec i 0 (concatD [prt 0 id_, doc (showString "--"), doc (showString ";")])
    Grammar.Abs.Ret expr -> prPrec i 0 (concatD [doc (showString "return"), prt 0 expr, doc (showString ";")])
    Grammar.Abs.VRet -> prPrec i 0 (concatD [doc (showString "return"), doc (showString ";")])
    Grammar.Abs.Cond expr stmt -> prPrec i 0 (concatD [doc (showString "if"), doc (showString "("), prt 0 expr, doc (showString ")"), prt 0 stmt])
    Grammar.Abs.CondElse expr stmt1 stmt2 -> prPrec i 0 (concatD [doc (showString "if"), doc (showString "("), prt 0 expr, doc (showString ")"), prt 0 stmt1, doc (showString "else"), prt 0 stmt2])
    Grammar.Abs.While expr stmt -> prPrec i 0 (concatD [doc (showString "while"), doc (showString "("), prt 0 expr, doc (showString ")"), prt 0 stmt])
    Grammar.Abs.SExp expr -> prPrec i 0 (concatD [prt 0 expr, doc (showString ";")])
    Grammar.Abs.FnInDef type_ id_ args block -> prPrec i 0 (concatD [prt 0 type_, prt 0 id_, doc (showString "("), prt 0 args, doc (showString ")"), prt 0 block])
    Grammar.Abs.ConstFor type_ id_ expr1 expr2 stmt -> prPrec i 0 (concatD [doc (showString "for"), doc (showString "("), prt 0 type_, prt 0 id_, doc (showString "="), prt 0 expr1, doc (showString "to"), prt 0 expr2, doc (showString ")"), prt 0 stmt])
    Grammar.Abs.Break -> prPrec i 0 (concatD [doc (showString "break"), doc (showString ";")])
    Grammar.Abs.Continue -> prPrec i 0 (concatD [doc (showString "continue"), doc (showString ";")])
    Grammar.Abs.Print expr -> prPrec i 0 (concatD [doc (showString "print"), doc (showString "("), prt 0 expr, doc (showString ")"), doc (showString ";")])
  prtList _ [] = concatD []
  prtList _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print Grammar.Abs.Item where
  prt i = \case
    Grammar.Abs.NoInit id_ -> prPrec i 0 (concatD [prt 0 id_])
    Grammar.Abs.Init id_ expr -> prPrec i 0 (concatD [prt 0 id_, doc (showString "="), prt 0 expr])
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [Grammar.Abs.Item] where
  prt = prtList

instance Print Grammar.Abs.Type where
  prt i = \case
    Grammar.Abs.Int -> prPrec i 0 (concatD [doc (showString "int")])
    Grammar.Abs.Str -> prPrec i 0 (concatD [doc (showString "string")])
    Grammar.Abs.Bool -> prPrec i 0 (concatD [doc (showString "boolean")])
    Grammar.Abs.Void -> prPrec i 0 (concatD [doc (showString "void")])
    Grammar.Abs.FuncType types type_ -> prPrec i 0 (concatD [doc (showString "fun"), doc (showString "("), prt 0 types, doc (showString "->"), prt 0 type_, doc (showString ")")])
  prtList _ [] = concatD []
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [Grammar.Abs.Type] where
  prt = prtList

instance Print Grammar.Abs.Expr where
  prt i = \case
    Grammar.Abs.EVar id_ -> prPrec i 6 (concatD [prt 0 id_])
    Grammar.Abs.ELitInt n -> prPrec i 6 (concatD [prt 0 n])
    Grammar.Abs.ELitTrue -> prPrec i 6 (concatD [doc (showString "true")])
    Grammar.Abs.ELitFalse -> prPrec i 6 (concatD [doc (showString "false")])
    Grammar.Abs.EApp id_ exprs -> prPrec i 6 (concatD [prt 0 id_, doc (showString "("), prt 0 exprs, doc (showString ")")])
    Grammar.Abs.EString str -> prPrec i 6 (concatD [prt 0 str])
    Grammar.Abs.ELambda args type_ block -> prPrec i 6 (concatD [doc (showString "lambda"), doc (showString "("), prt 0 args, doc (showString ")"), doc (showString ":"), prt 0 type_, prt 0 block])
    Grammar.Abs.Neg expr -> prPrec i 5 (concatD [doc (showString "-"), prt 6 expr])
    Grammar.Abs.Not expr -> prPrec i 5 (concatD [doc (showString "!"), prt 6 expr])
    Grammar.Abs.EMul expr1 mulop expr2 -> prPrec i 4 (concatD [prt 4 expr1, prt 0 mulop, prt 5 expr2])
    Grammar.Abs.EAdd expr1 addop expr2 -> prPrec i 3 (concatD [prt 3 expr1, prt 0 addop, prt 4 expr2])
    Grammar.Abs.ERel expr1 relop expr2 -> prPrec i 2 (concatD [prt 2 expr1, prt 0 relop, prt 3 expr2])
    Grammar.Abs.EAnd expr1 expr2 -> prPrec i 1 (concatD [prt 2 expr1, doc (showString "&&"), prt 1 expr2])
    Grammar.Abs.EOr expr1 expr2 -> prPrec i 0 (concatD [prt 1 expr1, doc (showString "||"), prt 0 expr2])
  prtList _ [] = concatD []
  prtList _ [x] = concatD [prt 0 x]
  prtList _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [Grammar.Abs.Expr] where
  prt = prtList

instance Print Grammar.Abs.AddOp where
  prt i = \case
    Grammar.Abs.Plus -> prPrec i 0 (concatD [doc (showString "+")])
    Grammar.Abs.Minus -> prPrec i 0 (concatD [doc (showString "-")])

instance Print Grammar.Abs.MulOp where
  prt i = \case
    Grammar.Abs.Times -> prPrec i 0 (concatD [doc (showString "*")])
    Grammar.Abs.Div -> prPrec i 0 (concatD [doc (showString "/")])
    Grammar.Abs.Mod -> prPrec i 0 (concatD [doc (showString "%")])

instance Print Grammar.Abs.RelOp where
  prt i = \case
    Grammar.Abs.LTH -> prPrec i 0 (concatD [doc (showString "<")])
    Grammar.Abs.LE -> prPrec i 0 (concatD [doc (showString "<=")])
    Grammar.Abs.GTH -> prPrec i 0 (concatD [doc (showString ">")])
    Grammar.Abs.GE -> prPrec i 0 (concatD [doc (showString ">=")])
    Grammar.Abs.EQU -> prPrec i 0 (concatD [doc (showString "==")])
    Grammar.Abs.NE -> prPrec i 0 (concatD [doc (showString "!=")])

